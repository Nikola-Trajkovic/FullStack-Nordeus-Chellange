# Define grid parameters
cell_size = 20  # Width and height of each cell in pixels
grid_size = 30  # Number of cells in each row and column
screen_size = cell_size * grid_size  # Total screen size

# Define colors
background_color = (0, 0, 0)   # Black background
rect_color = (0, 128, 255)     # Blue for rectangles